# Implementation Todo - 视频播放量追踪

## Preparation

### 环境准备
- [ ] 确认 Python 版本 >= 3.10
- [ ] 确认依赖已安装（APScheduler、ECharts CDN）
- [ ] 备份数据库文件 `data/monitor.db`

### 技术调研
- [x] B站视频信息API文档确认（已在 `src/bilibili.py` 中使用）
- [x] APScheduler 使用方式确认（已在稍后再看功能中使用）
- [x] ECharts 基本用法确认（社区成熟方案）

---

## Development Tasks

### 阶段1：数据库表设计和迁移

#### 任务1.1：创建数据库迁移脚本
- [ ] 创建 `scripts/migrate_add_video_stats_table.py`
- [ ] 定义 `video_stats` 表结构
- [ ] 创建索引：`idx_video_stats_video_id`, `idx_video_stats_recorded_at`
- [ ] 添加配置项到 `config` 表：
  - `stats_tracking_duration = 24`
  - `hot_video_threshold = 10000`
  - `hot_growth_rate_threshold = 50`
  - `enable_hot_notification = 1`
- [ ] 测试迁移脚本执行
- [ ] 备份并执行迁移

**预计耗时**：0.5小时

---

#### 任务1.2：实现数据库 CRUD 方法
- [ ] 在 `src/database.py` 中添加方法：
  - [ ] `create_video_stats_table()` - 创建表（在 `init_db()` 中调用）
  - [ ] `add_video_stats(video_id, stats)` - 添加统计记录
  - [ ] `get_video_stats(bvid, hours)` - 查询统计数据
  - [ ] `get_videos_pushed_after(cutoff_time)` - 查询推送后指定时间的视频
  - [ ] `get_latest_video_stats(video_id)` - 获取最新统计记录
  - [ ] `cleanup_old_video_stats(days)` - 清理过期数据
  - [ ] `get_hot_videos(threshold, limit)` - 查询热门视频列表
- [ ] 编写单元测试 `tests/test_video_stats_db.py`
- [ ] 运行测试确认功能正常

**预计耗时**：1小时

---

### 阶段2：定时任务实现

#### 任务2.1：实现数据采集定时任务
- [ ] 在 `src/scheduler.py` 中添加方法：
  - [ ] `setup_video_stats_tracker()` - 设置定时任务
  - [ ] `_track_video_stats()` - 执行数据采集
  - [ ] `_fetch_video_stats(bvid)` - 调用B站API获取数据
- [ ] 在 `start()` 方法中调用 `setup_video_stats_tracker()`
- [ ] 添加线程锁保护采集过程
- [ ] 控制并发数量（最多同时采集10个视频）

**预计耗时**：1.5小时

---

#### 任务2.2：实现错误处理和日志
- [ ] 处理B站API限流（返回码 -509）
- [ ] 处理视频已删除的情况
- [ ] 记录采集成功/失败日志
- [ ] 添加失败重试机制（最多2次）

**预计耗时**：0.5小时

---

### 阶段3：热门提醒推送

#### 任务3.1：实现阈值判断逻辑
- [ ] 在 `src/scheduler.py` 中添加方法：
  - [ ] `_check_hot_threshold(video_id, current_stats)` - 判断热门阈值
- [ ] 实现播放量绝对值阈值判断
- [ ] 实现增长率阈值判断
- [ ] 读取配置参数

**预计耗时**：0.5小时

---

#### 任务3.2：实现飞书热门提醒
- [ ] 在 `src/feishu.py` 中添加方法：
  - [ ] `send_hot_video_notification(video_title, video_url, play_count, growth_rate)`
- [ ] 设计飞书消息卡片（橙色主题）
- [ ] 包含：视频标题、播放量、增长率、查看按钮
- [ ] 测试飞书推送

**预计耗时**：0.5小时

---

#### 任务3.3：实现去重逻辑
- [ ] 在 `push_history` 表中记录热门提醒推送历史
- [ ] 实现检查逻辑：同一视频每小时最多推送1次
- [ ] 在 `_track_video_stats()` 中调用去重检查

**预计耗时**：0.5小时

---

### 阶段4：API 接口实现

#### 任务4.1：新增响应模型
- [ ] 在 `src/models.py` 中添加模型：
  - [ ] `VideoStatItem` - 统计项模型
  - [ ] `VideoStatsResponse` - 统计响应模型
  - [ ] `HotVideoItem` - 热门视频项模型
  - [ ] `HotVideosResponse` - 热门视频响应模型

**预计耗时**：0.25小时

---

#### 任务4.2：实现统计查询 API
- [ ] 在 `src/api/videos.py` 中添加端点：
  - [ ] `GET /api/videos/{bvid}/stats` - 获取视频统计数据
  - [ ] `GET /api/videos/hot` - 获取热门视频列表
- [ ] 添加参数验证（hours: 1-72, limit: 1-50）
- [ ] 添加错误处理
- [ ] 编写API文档注释

**预计耗时**：0.5小时

---

### 阶段5：前端可视化实现

#### 任务5.1：创建视频详情页模板
- [ ] 创建 `templates/video_detail.html`
- [ ] 包含：视频信息卡片、指标选择器、时间范围选择器、图表容器
- [ ] 使用 Tailwind CSS 样式
- [ ] 响应式设计（支持移动端）

**预计耗时**：1小时

---

#### 任务5.2：实现图表渲染逻辑
- [ ] 创建 `static/js/video_detail.js`
- [ ] 使用 ECharts 初始化图表
- [ ] 实现数据加载函数 `loadStats()`
- [ ] 实现图表渲染函数 `renderChart(stats)`
- [ ] 实现指标切换逻辑（播放量、点赞数等）
- [ ] 实现时间范围切换逻辑（1h, 6h, 12h, 24h）
- [ ] 添加图表响应式调整（窗口大小变化）

**预计耗时**：1.5小时

---

#### 任务5.3：修改推送历史页面
- [ ] 在 `templates/videos.html` 中添加"查看详情"入口
- [ ] 添加路由：点击视频标题或BV号跳转到详情页
- [ ] 添加路由处理：`/videos/{bvid}` 映射到详情页

**预计耗时**：0.5小时

---

#### 任务5.4：添加路由和导航
- [ ] 在 `src/web.py` 中添加路由：
  - [ ] `GET /videos/{bvid}` - 渲染视频详情页
- [ ] 确保路由参数正确传递到前端

**预计耗时**：0.25小时

---

### 阶段6：配置管理

#### 任务6.1：配置页面显示
- [ ] 在 `templates/config.html` 中添加追踪参数设置：
  - [ ] 追踪时长（1-72小时）
  - [ ] 热门播放量阈值
  - [ ] 增长率阈值
  - [ ] 是否启用热门提醒（开关）
- [ ] 使用表单组件保存配置

**预计耗时**：0.5小时

---

#### 任务6.2：配置保存逻辑
- [ ] 在 `src/api/config.py` 中添加配置保存逻辑
- [ ] 验证参数范围
- [ ] 配置保存后立即生效（无需重启服务）

**预计耗时**：0.25小时

---

## Testing Tasks

### 单元测试

#### 测试数据库 CRUD
- [ ] 测试 `add_video_stats()` 正常写入
- [ ] 测试 `get_video_stats()` 查询逻辑
- [ ] 测试 `get_videos_pushed_after()` 时间范围过滤
- [ ] 测试 `cleanup_old_video_stats()` 清理逻辑

**测试文件**：`tests/test_video_stats_db.py`

---

#### 测试阈值判断
- [ ] 测试播放量阈值判断
- [ ] 测试增长率阈值判断
- [ ] 测试未启用热门提醒的情况

**测试文件**：`tests/test_video_stats_threshold.py`

---

### 集成测试

#### 测试定时任务
- [ ] 模拟视频推送，验证T0时刻记录初始播放量
- [ ] 手动触发定时任务，验证数据采集和写入
- [ ] 模拟B站API限流，验证错误处理

**测试方法**：使用 Mock 对象模拟B站API

---

#### 测试热门提醒
- [ ] 模拟播放量达到阈值，验证飞书推送
- [ ] 验证去重逻辑（同一视频每小时最多推送1次）
- [ ] 验证未配置Webhook时的处理

**测试方法**：使用 Mock 对象模拟飞书推送

---

#### 测试 API 接口
- [ ] 测试 `GET /api/videos/{bvid}/stats` 返回正确数据
- [ ] 测试 `GET /api/videos/hot` 返回热门视频列表
- [ ] 测试参数验证（hours, limit）

**测试方法**：使用 FastAPI TestClient

---

### 手动测试

#### 功能测试
- [ ] 启动服务：`python main.py --web`
- [ ] 触发视频推送（手动推送或等待新视频）
- [ ] 查看数据库 `video_stats` 表是否有记录
- [ ] 等待热门提醒推送（可临时降低阈值测试）
- [ ] 访问视频详情页查看图表

---

#### 兼容性测试
- [ ] 测试移动端图表显示
- [ ] 测试不同浏览器的兼容性（Chrome, Firefox, Edge）
- [ ] 测试窗口大小变化时的响应式调整

---

#### 性能测试
- [ ] 模拟100个视频同时采集，验证性能
- [ ] 查询大量统计数据时的响应时间
- [ ] 数据库清理操作的性能影响

---

## Completion Checklist

### 代码完成度
- [ ] 所有代码文件已创建/修改
- [ ] 代码符合项目规范（命名、注释、日志）
- [ ] 无明显的性能问题
- [ ] 无安全问题（SQL注入、XSS等）

---

### 功能完整性
- [ ] 数据采集定时任务正常运行
- [ ] 热门提醒推送正常
- [ ] 数据可视化图表正常显示
- [ ] 配置管理功能正常

---

### 测试覆盖
- [ ] 单元测试通过
- [ ] 集成测试通过
- [ ] 手动测试通过

---

### 文档更新
- [ ] 更新 `README.md` 添加功能说明
- [ ] 更新 `CHANGELOG-monitor_onlineVideo.md` 记录版本变更
- [ ] 更新 `DEVLOG-monitor_onlineVideo.md` 记录开发过程

---

### 部署准备
- [ ] 数据库迁移脚本可重复执行
- [ ] 配置项说明文档
- [ ] 用户使用指南（如何查看视频详情页）

---

## 预计总耗时

| 阶段 | 任务 | 预计耗时 |
|------|------|---------|
| 阶段1 | 数据库表设计和迁移 | 1.5小时 |
| 阶段2 | 定时任务实现 | 2小时 |
| 阶段3 | 热门提醒推送 | 1.5小时 |
| 阶段4 | API 接口实现 | 0.75小时 |
| 阶段5 | 前端可视化实现 | 3.25小时 |
| 阶段6 | 配置管理 | 0.75小时 |
| 测试 | 单元测试、集成测试、手动测试 | 2小时 |
| 文档 | 更新README、CHANGELOG、DEVLOG | 0.5小时 |
| **总计** | | **11.25小时** |

---

## 依赖关系

```
阶段1 (数据库)
    ↓
阶段2 (定时任务)
    ↓
阶段3 (热门提醒)
    ↓
阶段4 (API接口)
    ↓
阶段5 (前端可视化)
    ↓
阶段6 (配置管理)
    ↓
测试
    ↓
文档
```

---

## 风险提示

### 高风险项
1. **B站API限流**：可能导致数据采集失败
   - 缓解措施：添加随机延迟、失败重试、监控API返回码

2. **数据库性能**：统计数据累积可能影响查询性能
   - 缓解措施：定期清理、添加索引、限制查询范围

### 中风险项
1. **并发安全**：定时任务与监控循环可能冲突
   - 缓解措施：使用线程锁保护、控制并发数量

2. **前端兼容性**：ECharts 在某些浏览器可能不兼容
   - 缓解措施：测试主流浏览器、提供降级方案

---

## 优先级说明

- **P0**：核心功能，必须完成
- **P1**：重要功能，建议完成
- **P2**：增值功能，可后续迭代
- **P3**：优化功能，可延后实现

建议按阶段顺序实施，优先完成阶段1-3（数据采集和热门提醒），阶段4-6可根据实际情况调整。