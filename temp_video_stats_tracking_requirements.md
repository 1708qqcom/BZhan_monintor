# Feature Requirements - 视频播放量追踪

## Background

当前系统在检测到新视频发布后，会立即推送通知到飞书，但缺少"推送后"的数据洞察。

用户反馈：
- 不知道视频推送后热度如何变化
- 难以发现潜在的爆款视频
- 缺少视频生命周期的数据支撑

## Goal

补充"推送后"的数据洞察，延伸产品价值：

1. **追踪热度变化**：记录视频推送后的播放量、点赞数、评论数等数据变化
2. **发现爆款视频**：当播放量突破阈值时，推送"热门提醒"
3. **可视化展示**：在视频详情页展示播放量曲线图
4. **了解生命周期**：帮助用户了解视频从发布到衰退的过程

## User Story

**As a** B站内容消费者

**I want** 了解推送视频的热度变化趋势

**So that** 我可以：
- 及时发现爆款视频，不错过热门内容
- 了解视频生命周期，优化观看时机
- 判断UP主影响力，调整关注策略

## Functional Requirements

### FR-001: 数据采集

**描述**：定时采集推送后视频的统计数据

**行为**：
- 对推送后的视频，每小时记录一次播放量、点赞数、评论数、投币数、收藏数、分享数、弹幕数
- 采集时长：推送后24小时内（可配置）
- 采集时间点：每小时整点（如 14:00, 15:00, 16:00...）

**数据来源**：B站视频信息 API (`/x/web-interface/view`)

**优先级**：P0

---

### FR-002: 数据存储

**描述**：持久化存储视频统计数据

**行为**：
- 每个视频每小时产生一条统计记录
- 记录包含：视频ID、播放量、点赞数、评论数、投币数、收藏数、分享数、弹幕数、记录时间
- 支持按视频ID查询历史统计

**存储周期**：统计数据保留7天，过期自动清理

**优先级**：P0

---

### FR-003: 热门提醒

**描述**：当视频播放量突破阈值时，推送热门提醒

**行为**：
- 阈值判断逻辑：
  - 播放量绝对值阈值：默认 10,000 次（可配置）
  - 增长率阈值：默认 50%（与上一小时对比，可配置）
- 触发条件：满足任意一个阈值
- 推送频率：同一视频每小时最多推送1次
- 推送内容：视频标题、当前播放量、增长率、查看链接

**推送渠道**：飞书群机器人 Webhook

**优先级**：P1

---

### FR-004: 数据可视化

**描述**：在视频详情页展示播放量曲线图

**行为**：
- 支持查看单个视频的统计数据
- 图表类型：折线图
- 支持切换指标：播放量、点赞数、评论数等
- 支持切换时间范围：1小时、6小时、12小时、24小时
- 响应式设计：支持移动端查看

**交互**：
- 在推送历史列表点击"查看详情"进入详情页
- 或点击视频标题直接跳转到详情页

**优先级**：P2

---

### FR-005: 配置管理

**描述**：支持用户自定义追踪参数

**行为**：
- 追踪时长：1-72小时（默认24小时）
- 热门提醒阈值：播放量绝对值阈值（默认10,000）
- 热门提醒增长率阈值：默认50%
- 开关控制：是否启用热门提醒

**配置入口**：Web管理后台 → 配置管理页面

**优先级**：P3

---

## User Flow

### 流程1：数据采集流程

```
视频推送成功
    ↓
记录初始播放量（T0时刻）
    ↓
定时任务触发（每小时）
    ↓
查询推送后24h内的视频列表
    ↓
调用B站API获取实时统计数据
    ↓
写入 video_stats 表
    ↓
判断是否达到热门阈值
    ↓
【是】→ 推送热门提醒
【否】→ 结束
```

### 流程2：用户查看数据

```
打开推送历史页面
    ↓
点击视频"查看详情"按钮
    ↓
进入视频详情页
    ↓
自动加载统计数据
    ↓
渲染播放量曲线图
    ↓
用户切换指标/时间范围
    ↓
图表动态更新
```

### 流程3：热门提醒推送

```
定时任务检测到播放量突破阈值
    ↓
构造飞书消息卡片
    ↓
发送到飞书群
    ↓
记录推送历史（避免重复推送）
    ↓
用户点击"查看视频"跳转到B站
```

---

## Edge Cases

### EC-001: B站API限流

**场景**：B站API返回频率限制错误（-509）

**处理**：
- 记录错误日志
- 跳过本次采集，等待下一次定时任务
- 不影响其他视频的数据采集

**优先级**：P1

---

### EC-002: 视频被删除

**场景**：视频在B站被删除，无法获取统计数据

**处理**：
- API返回错误（如视频不存在）
- 标记该视频为"已删除"
- 停止后续采集

**优先级**：P2

---

### EC-003: 数据库存储空间不足

**场景**：统计数据累积过多，占用大量存储空间

**处理**：
- 自动清理7天前的统计数据
- 提供手动清理功能（管理员）
- 在配置页面显示数据库大小

**优先级**：P2

---

### EC-004: 用户未配置飞书Webhook

**场景**：热门提醒功能启用，但未配置飞书Webhook

**处理**：
- 在配置页面提示用户配置
- 热门检测仍然运行，但不推送通知
- 记录日志：未配置Webhook，跳过推送

**优先级**：P2

---

### EC-005: 多用户并发采集

**场景**：多个用户同时监控视频，定时任务并发执行

**处理**：
- 使用线程锁保护采集过程
- 避免同一视频被重复采集
- 控制并发数量，避免API限流

**优先级**：P1

---

## Acceptance Criteria

### AC-001: 数据采集功能验收

- [ ] 推送新视频后，T0时刻记录初始播放量
- [ ] 定时任务每小时执行一次
- [ ] 每次采集记录播放量、点赞数、评论数、投币数、收藏数、分享数、弹幕数
- [ ] 数据写入 `video_stats` 表
- [ ] 采集时长满足配置要求（默认24小时）

---

### AC-002: 热门提醒功能验收

- [ ] 播放量达到阈值（如10,000）时触发提醒
- [ ] 增长率达到阈值（如50%）时触发提醒
- [ ] 飞书推送消息格式正确
- [ ] 同一视频每小时最多推送1次
- [ ] 未配置Webhook时不影响系统运行

---

### AC-003: 数据可视化功能验收

- [ ] 点击"查看详情"可进入视频详情页
- [ ] 播放量曲线图正确渲染
- [ ] 支持切换指标（播放量、点赞数等）
- [ ] 支持切换时间范围（1h, 6h, 12h, 24h）
- [ ] 移动端图表正常显示

---

### AC-004: 配置管理功能验收

- [ ] 配置页面显示追踪参数设置
- [ ] 支持修改追踪时长（1-72h）
- [ ] 支持修改热门提醒阈值
- [ ] 支持开关热门提醒功能
- [ ] 配置保存后立即生效

---

### AC-005: 数据清理功能验收

- [ ] 自动清理7天前的统计数据
- [ ] 清理过程不影响系统性能
- [ ] 提供手动清理入口（管理员）

---

## Success Metrics

1. **数据完整性**：推送后24小时内，每小时都有统计数据记录（API正常情况下）
2. **热门发现率**：播放量超过10,000的视频，90%被成功提醒
3. **系统稳定性**：定时任务成功率 > 95%
4. **用户满意度**：用户反馈此功能有价值（通过问卷或访谈）

---

## Dependencies

- B站视频信息 API (`/x/web-interface/view`)
- 飞书群机器人 Webhook
- 数据库存储空间（预估：每个视频24条记录 × 7天 = 168条/视频）
- APScheduler 定时任务库（已在项目中使用）

---

## Out of Scope

- 视频评论内容分析
- 弹幕情感分析
- UP主粉丝增长追踪
- 分区热度对比
- 数据导出功能（可作为后续迭代）
