# Feature Requirements: User Onboarding Flow

## Background

B站UP主监控系统是一个功能完整的监控工具，支持：
- 扫码登录B站账号
- 自动同步关注列表
- 定时检查新视频
- 飞书推送通知
- Web管理后台

**问题**：新用户首次访问系统时，面临以下挑战：
1. 不清楚需要完成哪些配置才能正常使用
2. 需要在多个页面之间跳转才能完成初始化
3. 缺乏明确的操作指引，容易中途放弃

**影响**：
- 用户上手成本高
- 首次使用失败率高
- 核心功能使用率低


## Goal

提供一个结构化的3步引导流程，让新用户在**5分钟内**完成核心配置，快速体验系统核心功能。

**成功指标**：
- 引导完成率 ≥ 70%
- 首次配置时间 ≤ 5分钟
- 新用户7日留存率提升 ≥ 20%


## User Story

**As a** 新注册用户

**I want** 一个清晰的引导流程，帮助我快速完成系统配置

**So that** 我能在5分钟内开始使用监控功能，而不是在各种页面间迷失


## Functional Requirements

### FR-001: 引导流程触发

**描述**：
- 用户注册成功后，自动跳转到引导页面（`/onboarding`）
- 老用户登录时，检查引导进度，未完成则跳转到对应步骤
- 用户可随时通过导航栏访问引导页

**优先级**：高


### FR-002: 引导进度跟踪

**描述**：
- 系统跟踪每个用户的引导进度（步骤完成状态、跳过状态）
- 每步完成后，自动更新进度并跳转下一步
- 支持中途退出，下次登录继续

**优先级**：高


### FR-003: 步骤1 - B站账号绑定

**描述**：
- 显示B站扫码登录界面
- 用户扫描二维码完成B站账号绑定
- 绑定成功后，自动同步关注列表
- 提供"跳过"按钮，跳过后在仪表盘显示提示

**验收标准**：
- 扫码成功后，用户B站账号绑定完成
- 同步关注列表成功（最多50个UP主）
- 跳过后，步骤标记为"已跳过"

**优先级**：高


### FR-004: 步骤2 - 飞书推送配置

**描述**：
- 显示飞书Webhook配置表单
- 提供图文教程链接（如何获取Webhook地址）
- 提供"测试推送"按钮验证配置
- 提供"跳过"按钮，跳过后提示"将无法收到推送通知"

**验收标准**：
- 填写Webhook地址并测试成功
- 跳过后，步骤标记为"已跳过"

**优先级**：中


### FR-005: 步骤3 - UP主选择

**描述**：
- 显示已同步的UP主列表（来自步骤1）
- 默认全选，用户可取消勾选
- 支持搜索和筛选
- 提供"跳过"按钮（如果步骤1跳过，此步骤自动跳过并提示）

**验收标准**：
- 用户选择的UP主添加到监控列表
- 跳过后，使用默认同步的UP主列表

**优先级**：中


### FR-006: 引导完成与跳转

**描述**：
- 所有步骤完成后，显示"配置完成"祝贺页面
- 提供"开始使用"按钮跳转到仪表盘
- 仪表盘显示引导完成状态

**优先级**：高


### FR-007: 仪表盘进度提示

**描述**：
- 引导未完成的用户，在仪表盘顶部显示进度提示卡片
- 显示内容："配置进度 33%，点击继续完成配置"
- 点击卡片跳转到引导页对应步骤

**优先级**：中


### FR-008: 跳过引导

**描述**：
- 每个步骤都提供"跳过"按钮
- 跳过后，步骤标记为"已跳过"，不影响后续步骤
- 跳过的步骤可在引导页重新完成

**优先级**：中


## User Flow

### 主流程

```
用户注册
    ↓
跳转到 /onboarding
    ↓
显示引导页面（步骤1）
    ↓
[步骤1：B站登录]
    ├─ 完成扫码 → 自动跳转步骤2
    └─ 点击跳过 → 记录状态，跳转步骤2
    ↓
[步骤2：飞书配置]
    ├─ 填写Webhook → 测试成功 → 跳转步骤3
    └─ 点击跳过 → 记录状态，跳转步骤3
    ↓
[步骤3：UP主选择]
    ├─ 选择UP主 → 点击完成 → 跳转仪表盘
    └─ 点击跳过 → 使用默认列表 → 跳转仪表盘
    ↓
仪表盘显示"配置完成"
```

### 中断恢复流程

```
用户登录
    ↓
检查引导进度
    ├─ 未开始 → 跳转步骤1
    ├─ 步骤1完成，步骤2未完成 → 跳转步骤2
    ├─ 步骤1、2完成，步骤3未完成 → 跳转步骤3
    └─ 全部完成或全部跳过 → 进入仪表盘
```


## Edge Cases

### EC-001: 老用户兼容性

**场景**：老用户数据库中没有引导记录

**处理**：
- 检查 `user_onboarding` 表是否存在记录
- 不存在则默认标记为"已完成引导"
- 或在数据库迁移时为所有老用户创建"已完成"记录


### EC-002: 步骤依赖关系

**场景**：用户跳过步骤1，无法执行步骤3

**处理**：
- 如果步骤1跳过，步骤3自动标记为"无法完成"
- 显示提示："请先完成B站账号绑定"
- 在仪表盘显示引导卡片，提示用户完成步骤1


### EC-003: 引导流程中断

**场景**：用户完成步骤1后关闭浏览器

**处理**：
- 每步完成后立即保存状态到数据库
- 下次登录时检查 `current_step`，跳转到对应步骤


### EC-004: 管理员账号

**场景**：管理员是否需要引导流程？

**建议**：
- 管理员也需要引导流程
- 管理员可以跳过任何步骤
- 或提供"管理员快速配置"模式（自动完成所有步骤）


### EC-005: 移动端适配

**场景**：移动端扫码和UP主选择体验

**处理**：
- 二维码适配移动端尺寸
- UP主列表使用虚拟滚动优化性能
- 提供"桌面端继续"提示


## Acceptance Criteria

### AC-001: 引导触发

- [ ] 新用户注册后自动跳转到 `/onboarding`
- [ ] 未完成引导的用户登录后跳转到对应步骤
- [ ] 引导页面不在导航栏显示（或显示为"继续引导"）


### AC-002: 步骤1验证

- [ ] 扫码成功后，用户B站账号绑定完成
- [ ] 同步关注列表成功，UP主数量显示在页面
- [ ] 跳过按钮可用，点击后状态正确记录


### AC-003: 步骤2验证

- [ ] Webhook配置表单可用
- [ ] 测试推送功能正常
- [ ] 教程链接可点击跳转


### AC-004: 步骤3验证

- [ ] UP主列表正确显示（来自同步数据）
- [ ] 复选框交互正常
- [ ] 搜索和筛选功能可用


### AC-005: 完成流程

- [ ] 所有步骤完成后显示祝贺页面
- [ ] 跳转到仪表盘，进度显示100%
- [ ] 引导页面不再显示（或显示为"配置完成"）


### AC-006: 仪表盘提示

- [ ] 未完成引导的用户看到进度卡片
- [ ] 点击卡片跳转到对应步骤
- [ ] 完成引导后卡片消失


### AC-007: 数据持久化

- [ ] 引导进度正确保存到数据库
- [ ] 刷新页面后状态不丢失
- [ ] 跳过的步骤状态正确记录


## Non-Functional Requirements

### NFR-001: 性能

- 引导页面加载时间 ≤ 2秒
- API响应时间 ≤ 500ms
- 步骤切换动画流畅（60fps）


### NFR-002: 可用性

- 引导流程步骤清晰，用户无需查看文档
- 错误提示友好，提供解决方案
- 跳过功能显眼，用户不会卡住


### NFR-003: 兼容性

- 支持主流浏览器（Chrome、Firefox、Safari、Edge）
- 移动端响应式设计
- 不影响老用户使用


## Out of Scope

- 个性化引导（根据用户角色定制步骤）
- 视频教程嵌入
- 多语言支持
- 引导流程A/B测试
